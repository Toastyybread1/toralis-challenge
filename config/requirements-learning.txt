# Optional neural training dependency; keep the classical runtime independent.
# GPU installation: pip install -r requirements-learning.txt --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
